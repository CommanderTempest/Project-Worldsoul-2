import Roact from "@rbxts/roact";

interface UIProperties {
  // is exactly what it sounds like, custom properties!
}

class HotbarUI extends Roact.Component<UIProperties>
{
  render()
  {
    return (
      <screengui>
      </screengui>
    )
  }
}